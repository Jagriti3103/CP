#include <bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cin>>s;
    set <char> str;
    for(auto &c: s){
        str.insert(c);
    }
    if(str.size()%2 == 0){
        cout<<"CHAT WITH HER!";
    }else{
        cout<<"IGNORE HIM!";
    }
    return 0;
}
